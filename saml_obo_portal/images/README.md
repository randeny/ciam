# Images

Screenshots referenced by the project [README](../README.md).

| File | Description |
| --- | --- |
| `cors-error.png` | Browser DevTools showing the native-auth `initiate` request blocked by CORS policy ("Failed to fetch"). |
| `afd-rule-origin-condition.png` | Azure Front Door rule condition matching the request URL (`<native-auth-host>`) and the browser `Origin` header. |
| `afd-rule-cors-rewrite.png` | Azure Front Door rule action overwriting the `Access-Control-Allow-Origin` and `Access-Control-Allow-Credentials` response headers. |

Save the three screenshots into this folder using the exact filenames above.
